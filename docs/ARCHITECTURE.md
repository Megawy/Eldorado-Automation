# Architecture
The Angular application provides the seller control panel. Browser-extension code is isolated under `src/extension`, shared contracts live in `src/models`, and feature routes lazy-load independently.
