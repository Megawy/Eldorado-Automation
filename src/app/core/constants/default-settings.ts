import { AutomationSettings } from '../../../models/settings.model';
export const DEFAULT_AUTOMATION_SETTINGS: AutomationSettings = { enabled: true, autoSendOffers: true, autoReply: true, responseDelaySeconds: 30 };
